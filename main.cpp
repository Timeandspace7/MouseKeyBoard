/**
******************************************************************************
* @file    main.cpp
* @author  Timeandspace7
* @func    鼠标与键盘模拟程序，详见下方链接
* @LinkS   https://www.processon.com/view/link/60117543e0b34d1f2ccd85ec
* @version V1.0.0
* @date    27-January-2021
* @place   Xuzhou,JiangSu
* @brief
******************************************************************************
*/

#include <iostream>
#include "main.h"

using namespace std;

void Test(); // 函数测试

int main()
{
	Test();

	cout << "执行完成！" << endl;
	system("pause");
	return 0;
}

void Test()
{
	//MoveAndLeftDoubleClick();	// @brief	：交互式输入坐标指定鼠标按下的位置并左键双击
	//Ctrl_C();				// @brief	：模拟 Ctrl + C
	//Ctrl_V();				// @brief	：模拟 Ctrl + V
	//MySetKeyBig();
	//MouseMove(1018, 341);	// @brief	：鼠标移动到指定位置
	//MouseLeftDoubleClick();// @brief	：鼠标左键双击
	//MouseLeftDown();	// @brief	：鼠标左键按下
	//MouseMove(1092, 322);
	//MouseLeftUp();		// @brief	：鼠标左键放开
	//MouseRightDown();	// @brief	：鼠标右键按下
	//MouseRightUp();	// @brief	：鼠标右键放开

	////MoveAndLeftClick();	// @brief	：设置鼠标按下的位置并左键点击
	//MouseMove(219, 749);	// @brief	：鼠标移动到指定位置
	//MouseLeftDown();	// @brief	：鼠标左键按下
	//MouseLeftUp();		// @brief	：鼠标左键放开
	//					//int xfixStep = 26;// y 轴移动像素
	//int yfixStep = 26;	// y 轴移动像素
	//int x = 162;	// 第一次需要点击的链接位置
	//int y = 538;	// 第一次需要点击的链接位置

	//MouseMove(x, y);	// @brief	：鼠标移动到指定位置
	//MouseRightDown();	// @brief	：鼠标右键按下
	//MouseRightUp();	// @brief	：鼠标右键放开
	//Sleep(10);
	//x += 52;
	//y -= 297;
	//MouseMove(x, y);


	///*for (int i = 0; i < 3; i++) {
	//MouseMove(x, y);	// @brief	：鼠标移动到指定位置
	////MouseLeftDown();	// @brief	：鼠标左键按下
	////MouseLeftUp();		// @brief	：鼠标左键放开
	//MouseRightDown();	// @brief	：鼠标右键按下
	//MouseRightUp();	// @brief	：鼠标右键放开
	//MouseMove(x+52, y-297);
	//MouseLeftDown();	// @brief	：鼠标左键按下
	//MouseLeftUp();		// @brief	：鼠标左键放开
	////x += fixStep;
	//y += yfixStep;
	//}*/

	//// 当程序运行1.5s时再执行模拟按键，以便选择需要操作的窗口
	//Sleep(1500);
	//MyAnalogKey("Love life,Love BiaoGe.0123456789");

	//MouseLeftDoubleClick();	// @brief	：鼠标左键双击

}